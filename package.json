{
  "name": "tower",
  "module": "server/index.ts",
  "type": "module",
  "scripts": {
    "build": "cd client && bun install && bun run build",
    "start": "bun run server/index.ts",
    "dev": "bun run start"
  },
  "devDependencies": {
    "@types/bun": "^1.1.13"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "nanoid": "^5.0.8"
  }
}
